# Student Management System with Kafka Integration

Этот проект состоит из двух Spring Boot сервисов на Kotlin:
1. **Student Service** - основной сервис для управления студентами и их хобби (порт 8080)
2. **Hobby Changer Service** - сервис для изменения хобби студентов (порт 8081)

## Архитектура

- **PostgreSQL** - база данных для хранения студентов и хобби
- **Kafka** - для обмена сообщениями между сервисами
- **Thymeleaf** - для веб-интерфейса

## Структура БД

```
students
  - id (PK)
  - name
  - created_at
  - updated_at

hobbies
  - id (PK)
  - name
  - student_id (FK)
  - created_at
  - updated_at
```

## Требования

- Java 17 или выше (рекомендуется Java 17)
- Docker и Docker Compose (для PostgreSQL и Kafka)
- Gradle 8.5 (автоматически загружается через wrapper)

## Запуск

### 0. Настройка JDK в IntelliJ IDEA

**ВАЖНО**: Если вы видите ошибки с JDK, следуйте инструкциям в файле `SETUP.md`

Кратко: File → Settings → Build Tools → Gradle → Gradle JVM → выберите Java 17

### 1. Запустите инфраструктуру (PostgreSQL и Kafka)

```bash
docker-compose up -d
```

Подождите несколько секунд, пока сервисы запустятся.

### 2. Запустите Student Service

В Windows:
```bash
gradlew.bat bootRun
```

В Linux/Mac:
```bash
./gradlew bootRun
```

Сервис будет доступен на http://localhost:8080

### 3. Запустите Hobby Changer Service

В новом терминале:

Windows:
```bash
cd hobby-changer
..\gradlew.bat bootRun
```

Linux/Mac:
```bash
cd hobby-changer
../gradlew bootRun
```

Сервис будет доступен на http://localhost:8081

### Альтернативный способ (оба сервиса параллельно)

Windows:
```bash
start-services.bat
```

PowerShell:
```powershell
.\start-services.ps1
```

## Использование

### Веб-интерфейс

Откройте http://localhost:8080/students в браузере. Вы можете:
- Просматривать список всех студентов
- Фильтровать студентов по имени или хобби
- Создавать новых студентов с хобби
- Изменять хобби студентов, нажав кнопку 🔄 рядом с хобби (отправляет запрос через Kafka)

### API Endpoints

#### Student Service (порт 8080)

- `GET /students` - веб-страница со списком студентов
- `GET /students/api?name=John&hobby=Reading` - API для получения студентов (с фильтрами)
- `POST /students/api` - создать нового студента (JSON)
- `POST /api/hobby-change/{studentId}?hobbyName=Reading` - запросить изменение хобби

#### Пример создания студента через API:

```bash
curl -X POST http://localhost:8080/students/api \
  -H "Content-Type: application/json" \
  -d '{"name": "Иван", "hobbies": ["Чтение", "Плавание"]}'
```

#### Пример запроса изменения хобби:

```bash
curl -X POST "http://localhost:8080/api/hobby-change/1?hobbyName=Чтение"
```

## Как работает обмен сообщениями через Kafka

1. Student Service отправляет сообщение в топик `hobby-change` с информацией о студенте и текущем хобби
2. Hobby Changer Service получает сообщение, генерирует новое случайное хобби и отправляет обновленное сообщение обратно в топик
3. Student Service получает обновленное сообщение и сохраняет новое хобби в БД

## Технологии

- Spring Boot 3.2.0
- Kotlin 1.9.22
- Gradle 8.5
- PostgreSQL 15
- Apache Kafka
- Thymeleaf
- Spring Data JPA
- Spring Kafka

## Решение проблем

Если вы видите ошибки при сборке проекта, особенно связанные с JDK или Gradle, см. файл `SETUP.md` для подробных инструкций.

