# 📋 Пошаговая инструкция по запуску проекта

## Шаг 1: Проверка требований

Убедитесь, что у вас установлены:
- ✅ Java 17 или выше (проверить: `java -version`)
- ✅ Docker Desktop (для PostgreSQL и Kafka)
- ✅ IntelliJ IDEA (или другой редактор)

## Шаг 2: Настройка JDK в IntelliJ IDEA (если используете IDEA)

1. Откройте IntelliJ IDEA
2. **File** → **Settings** (или `Ctrl + Alt + S`)
3. Перейдите в **Build, Execution, Deployment** → **Build Tools** → **Gradle**
4. В поле **"Gradle JVM"** выберите **Java 17** (или Project SDK)
5. Нажмите **OK**

Также проверьте Project SDK:
1. **File** → **Project Structure** (или `Ctrl + Alt + Shift + S`)
2. **Project** → **SDK**: выберите **Java 17**
3. Нажмите **OK**

## Шаг 3: Запуск инфраструктуры (PostgreSQL + Kafka)

Откройте терминал (PowerShell или CMD) в корне проекта и выполните:

```bash
docker-compose up -d
```

Это запустит:
- PostgreSQL на порту **5432**
- Zookeeper на порту **2181**
- Kafka на порту **9092**

Подождите 10-20 секунд, пока все контейнеры запустятся. Проверить статус можно командой:

```bash
docker-compose ps
```

Все контейнеры должны быть в статусе "Up".

## Шаг 4: Запуск первого сервиса (Student Service)

### Вариант А: Через терминал

Откройте первый терминал и выполните:

```bash
gradlew.bat bootRun
```

Подождите, пока увидите сообщение типа:
```
Started StudentServiceApplication in X.XXX seconds
```

Сервис будет доступен на: **http://localhost:8080**

### Вариант Б: Через IntelliJ IDEA

1. Найдите файл `StudentServiceApplication.kt` в папке `src/main/kotlin/com/practika/student/`
2. Правой кнопкой на файл → **Run 'StudentServiceApplication'**
3. Дождитесь запуска

## Шаг 5: Запуск второго сервиса (Hobby Changer Service)

### Вариант А: Через терминал

Откройте **второй терминал** (новое окно) и выполните:

```bash
cd hobby-changer
..\gradlew.bat bootRun
```

Подождите запуска. Сервис будет доступен на: **http://localhost:8081**

### Вариант Б: Через IntelliJ IDEA

1. Найдите файл `HobbyChangerApplication.kt` в папке `hobby-changer/src/main/kotlin/com/practika/hobbychanger/`
2. Правой кнопкой → **Run 'HobbyChangerApplication'**
3. Дождитесь запуска

### Вариант В: Запуск обоих сервисов одной командой

Можно использовать готовый скрипт:

**PowerShell:**
```powershell
.\start-services.ps1
```

**CMD:**
```cmd
start-services.bat
```

Это откроет два окна терминала с запущенными сервисами.

## Шаг 6: Открыть веб-интерфейс

Откройте браузер и перейдите по адресу:

**http://localhost:8080/students**

Вы увидите веб-страницу для управления студентами.

## Шаг 7: Тестирование

### Создание студента

1. На веб-странице заполните форму "Add New Student":
   - **Student Name**: например, "Иван"
   - **Hobbies**: например, "Reading, Swimming, Coding"
2. Нажмите **"Create Student"**
3. Студент появится в таблице

### Фильтрация

1. В форме "Filter Students" введите:
   - Имя для поиска по имени
   - Название хобби для поиска по хобби
2. Нажмите **"Filter"**

### Изменение хобби через Kafka

1. Найдите студента в таблице
2. Рядом с любым хобби нажмите кнопку **🔄**
3. Через несколько секунд обновите страницу (F5)
4. Хобби должно измениться на случайное!

**Что происходит:**
- Student Service отправляет сообщение в Kafka
- Hobby Changer Service получает его и генерирует новое хобби
- Hobby Changer отправляет обратно обновленное сообщение
- Student Service получает и сохраняет новое хобби в БД

## Остановка сервисов

### Остановка Spring Boot приложений

В терминалах нажмите `Ctrl + C`

### Остановка Docker контейнеров

```bash
docker-compose down
```

Или остановить и удалить данные:

```bash
docker-compose down -v
```

## Проверка логов

### Логи Docker контейнеров

```bash
# Все контейнеры
docker-compose logs

# Только PostgreSQL
docker-compose logs postgres

# Только Kafka
docker-compose logs kafka

# Следить за логами в реальном времени
docker-compose logs -f
```

### Логи Spring Boot приложений

Логи выводятся в терминале, где запущено приложение. Вы увидите:
- Сообщения о запуске
- Запросы к БД (если `show-sql: true` в application.yml)
- Kafka сообщения (отправка/получение)

## Решение проблем

### Ошибка "Port already in use"

Если порт 8080 или 8081 занят:
- Остановите другие приложения на этих портах
- Или измените порт в `application.yml`:
  ```yaml
  server:
    port: 8082  # вместо 8080
  ```

### Ошибка подключения к PostgreSQL

Проверьте, что Docker контейнер запущен:
```bash
docker-compose ps
```

Если не запущен:
```bash
docker-compose up -d postgres
```

### Ошибка подключения к Kafka

Проверьте, что Kafka и Zookeeper запущены:
```bash
docker-compose ps
```

### Ошибки компиляции в IntelliJ IDEA

1. **File** → **Invalidate Caches / Restart**
2. Выберите **"Invalidate and Restart"**
3. После перезапуска: **File** → **Reload Gradle Project**

### База данных не создается автоматически

Проверьте логи PostgreSQL:
```bash
docker-compose logs postgres
```

Hibernate должен автоматически создать таблицы при первом запуске.

## Готово! 🎉

Теперь оба сервиса работают и обмениваются сообщениями через Kafka!


