# ✅ Правильный запуск проекта

## Шаг 1: Запустите Docker контейнеры

```bash
docker-compose up -d
```

**Подождите 10-15 секунд** для инициализации PostgreSQL!

## Шаг 2: Проверьте статус контейнеров

```bash
docker-compose ps
```

Должны быть все в статусе "Up" и "healthy".

## Шаг 3: Запустите приложение

```bash
gradlew.bat bootRun
```

**НЕ ЗАКРЫВАЙТЕ** этот терминал! Приложение должно работать в нем.

Вы должны увидеть:
```
Started StudentServiceApplicationKt in X.XXX seconds
```

## Шаг 4: Откройте браузер

Перейдите на: **http://localhost:8080/students**

---

## ⚠️ Важно

1. **Docker контейнеры должны быть запущены ПЕРЕД запуском приложения**
2. **Не закрывайте терминал, где запущено приложение**
3. **Если остановили контейнеры (`docker-compose down`), запустите их снова**

---

## 🔍 Проверка

Если приложение не запускается, проверьте:

1. **PostgreSQL запущен:**
   ```bash
   docker-compose ps
   ```

2. **Подключение к PostgreSQL работает:**
   ```bash
   docker exec practika3-postgres psql -U student -d studentdb -c "SELECT 1;"
   ```

3. **Порт 8080 свободен:**
   ```bash
   netstat -ano | findstr :8080
   ```
   Если порт занят, остановите другое приложение или измените порт в `application.yml`.


