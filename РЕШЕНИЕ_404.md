# Решение проблемы "Страница не найдена" (404)

## Проблема
При переходе на `http://localhost:8080/students` видите "Страница не найдена" или 404 ошибку.

## Причины и решения

### ✅ Причина 1: Приложение не запущено

**Проверка:**
```bash
netstat -ano | findstr :8080
```
Если команда ничего не выводит - приложение не запущено.

**Решение:**
1. Убедитесь, что Docker контейнеры запущены:
   ```bash
   docker-compose ps
   ```

2. Запустите приложение:
   ```bash
   gradlew.bat bootRun
   ```

3. Подождите 15-20 секунд, пока увидите:
   ```
   Started StudentServiceApplicationKt in X.XXX seconds
   ```

4. Откройте браузер: http://localhost:8080/students

---

### ✅ Причина 2: Приложение еще запускается

**Решение:**
Подождите еще 10-20 секунд после запуска `gradlew.bat bootRun`. Spring Boot приложению нужно время для:
- Подключения к PostgreSQL
- Создания таблиц (если их нет)
- Инициализации всех компонентов

---

### ✅ Причина 3: Ошибка при запуске

**Проверка:**
Посмотрите в терминал, где запущено `gradlew.bat bootRun`. Если видите ошибки:
- Ошибки подключения к PostgreSQL → проверьте `docker-compose ps`
- Ошибки компиляции → проверьте код
- Другие ошибки → читайте сообщение об ошибке

---

### ✅ Причина 4: Неправильный URL

**Правильные URL:**
- ✅ http://localhost:8080/students
- ✅ http://127.0.0.1:8080/students

**Неправильные URL:**
- ❌ http://localhost:8080/student (без 's')
- ❌ http://localhost:8080/ (корневой путь)

---

## 🔍 Пошаговая диагностика

### Шаг 1: Проверьте Docker
```bash
docker-compose ps
```
Все должны быть "Up" и "healthy".

### Шаг 2: Проверьте, запущено ли приложение
```bash
netstat -ano | findstr :8080
```
Должна быть строка с `:8080`.

### Шаг 3: Проверьте логи приложения
В терминале, где запущено `gradlew.bat bootRun`, ищите:
- `Started StudentServiceApplicationKt` - приложение запустилось
- Ошибки - что-то пошло не так

### Шаг 4: Попробуйте API endpoint
```bash
curl http://localhost:8080/students/api
```
Если это работает, но `/students` не работает - проблема с Thymeleaf.

---

## ✅ Быстрое решение

1. **Остановите приложение** (если запущено): `Ctrl+C` в терминале

2. **Запустите Docker:**
   ```bash
   docker-compose up -d
   ```

3. **Подождите 10 секунд**

4. **Запустите приложение:**
   ```bash
   gradlew.bat bootRun
   ```

5. **Подождите 20 секунд**

6. **Откройте:** http://localhost:8080/students

---

## 📝 Важно

- **НЕ закрывайте терминал**, где запущено приложение
- **Docker должен быть запущен** перед запуском приложения
- **Подождите достаточно времени** для полного запуска


