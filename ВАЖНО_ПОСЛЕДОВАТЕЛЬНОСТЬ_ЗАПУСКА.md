# ⚠️ ВАЖНО: Правильная последовательность запуска

## Проблема
Если вы видите ошибку:
```
org.postgresql.util.PSQLException: ОШИБКА: аутентификация пользователя "student" не удалась
```

Это означает, что **PostgreSQL не запущен** или еще не готов.

## ✅ Правильная последовательность

### 1. Сначала запустите Docker контейнеры

```bash
docker-compose up -d
```

**ВАЖНО:** Подождите 10-15 секунд после этой команды, чтобы PostgreSQL полностью инициализировался!

### 2. Проверьте, что контейнеры запущены

```bash
docker-compose ps
```

Все контейнеры должны быть в статусе:
- **postgres**: `Up ... (healthy)`
- **kafka**: `Up ... (healthy)`
- **zookeeper**: `Up ...`

### 3. Только после этого запускайте приложение

```bash
gradlew.bat bootRun
```

---

## 🔄 Если вы остановили контейнеры

Если вы выполнили `docker-compose down` или `docker-compose down -v`, **обязательно** запустите их снова перед запуском приложения:

```bash
docker-compose up -d
# Подождите 10 секунд
gradlew.bat bootRun
```

---

## ✅ Быстрая проверка

Перед запуском приложения проверьте подключение к PostgreSQL:

```bash
docker exec practika3-postgres psql -U student -d studentdb -c "SELECT 1;"
```

Если команда выполнилась без ошибок - PostgreSQL готов к работе!

---

## 📝 Запомните

**ВСЕГДА сначала Docker, потом приложение!**

1. `docker-compose up -d` ← сначала это
2. Подождать 10 секунд
3. `gradlew.bat bootRun` ← потом это


