# Решение проблемы аутентификации Docker Hub

## Проблема
```
Error response from daemon: authentication required - email must be verified before using account
```

## ✅ Решение 1: Войти в Docker Hub через Docker Desktop (РЕКОМЕНДУЕТСЯ)

### Шаг 1: Создать аккаунт Docker Hub (если нет)

1. Перейдите на https://hub.docker.com/signup
2. Создайте бесплатный аккаунт
3. **ВАЖНО:** Проверьте email и подтвердите аккаунт (проверьте почту)

### Шаг 2: Войти в Docker Desktop

1. Откройте **Docker Desktop**
2. Нажмите на иконку настроек (⚙️) в правом верхнем углу
3. Или откройте меню (три точки) → **Settings**
4. Перейдите в раздел **"Sign in"** или **"Account"**
5. Нажмите **"Sign in"** и введите ваши данные Docker Hub
6. После входа Docker Desktop перезапустится автоматически

### Шаг 3: Попробовать снова

```bash
docker-compose up -d
```

---

## ✅ Решение 2: Войти через командную строку

```bash
docker login
```

Введите ваши данные Docker Hub:
- Username: ваш логин
- Password: ваш пароль (или Personal Access Token)

После успешного входа попробуйте:

```bash
docker-compose up -d
```

---

## ✅ Решение 3: Использовать альтернативные публичные образы

Если не хотите создавать аккаунт, можно использовать альтернативные репозитории (но это потребует изменения docker-compose.yml).

---

## 🔍 Проверка входа

Проверить, авторизованы ли вы:

```bash
docker info | findstr Username
```

Или в Docker Desktop: Settings → Account - должен быть виден ваш логин.

---

## ⚠️ Важно

После входа в Docker Hub, образы будут скачиваться без ограничений. Docker Hub предоставляет:
- **Бесплатный аккаунт**: 200 pulls каждые 6 часов для неавторизованных пользователей
- **После входа**: больше возможностей для скачивания

Образы `postgres:15` и `confluentinc/cp-zookeeper:7.5.0` - публичные, но Docker Hub может требовать авторизацию для предотвращения злоупотреблений.


