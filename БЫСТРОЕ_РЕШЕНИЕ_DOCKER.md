# 🚨 Быстрое решение проблемы с Docker

## Проблема
```
unable to get image 'postgres:15': error during connect: 
open //./pipe/dockerDesktopLinuxEngine: The system cannot find the file specified.
```

## ✅ Решение (выполните по порядку)

### Шаг 1: Полностью закрыть Docker Desktop

1. Найдите иконку Docker в **трее** (правый нижний угол, рядом с часами)
2. Правой кнопкой на иконку Docker
3. Выберите **"Quit Docker Desktop"** (Выход)
4. Подождите 10-15 секунд

### Шаг 2: Запустить Docker Desktop заново

1. Откройте меню **Пуск**
2. Найдите **"Docker Desktop"**
3. Запустите приложение
4. **ВАЖНО:** Дождитесь полного запуска (может занять 30-60 секунд)
5. В трее иконка Docker должна стать **зеленой** или показать статус "Running"

### Шаг 3: Проверить, что Docker работает

Откройте PowerShell или CMD и выполните:

```bash
docker ps
```

Если команда выполнилась **без ошибок** (даже если список пустой), Docker работает!

### Шаг 4: Запустить docker-compose

```bash
docker-compose up -d
```

---

## 🔍 Альтернативные решения

### Если Docker Desktop не запускается:

1. **Перезагрузите компьютер**
2. **Запустите Docker Desktop от имени администратора:**
   - Правой кнопкой на ярлык Docker Desktop
   - "Запуск от имени администратора"

### Если Docker Desktop зависает при запуске:

1. Откройте **Диспетчер задач** (Ctrl + Shift + Esc)
2. Найдите все процессы с "Docker" в названии
3. Завершите их все
4. Запустите Docker Desktop снова

### Если ничего не помогает:

1. **Переустановите Docker Desktop:**
   - Скачайте с https://www.docker.com/products/docker-desktop/
   - Удалите старую версию через Панель управления
   - Установите новую версию
   - Перезагрузите компьютер

---

## ⏱️ Время ожидания

После запуска Docker Desktop подождите **минимум 30-60 секунд** перед выполнением команд. Docker Desktop должен полностью инициализироваться.

---

## ✅ Проверка успешного запуска

Docker Desktop работает правильно, если:

1. ✅ Иконка Docker в трее зеленая или показывает "Running"
2. ✅ Команда `docker ps` выполняется без ошибок
3. ✅ В Docker Desktop видно сообщение "Docker Desktop is running"


